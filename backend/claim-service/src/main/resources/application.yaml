spring:
  application:
    name: claim-service

  datasource:
    url: jdbc:mysql://localhost:3306/claim_db
    username: root
    password: Mkshkumar@1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082